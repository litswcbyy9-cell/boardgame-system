# 骰子猫桌游馆系统 5 分钟公网答辩视频台本

> 演示定位：以公网服务器 `https://lhywork.top/` 实机演示为主，代码架构展示为辅。
> 讲解目标：用 5 分钟讲清楚系统具备线上部署、顾客预约、后台运营、会员、桌游、租借、权限、报表、AI 经营分析和工程化运维能力。

## 录制前准备

录制前在服务器执行一次更新和演示数据补齐：

```bash
cd /opt/boardgame-system
git pull origin main
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d --build
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod exec api npm run demo:seed
```

准备浏览器标签页：

- 顾客首页：`https://lhywork.top/`
- 后台入口：`https://lhywork.top/admin`
- 健康检查：`https://lhywork.top/api/health`

准备演示账号：

- 后台店长账号：使用服务器已有店长账号。
- 顾客演示账号：`19900061001 / demo12345`

## 时间轴台本

| 时间 | 演示画面 | 讲解台词 |
| --- | --- | --- |
| 00:00-00:15 | 打开 `https://lhywork.top/`，停留在顾客预约首页 | “老师好，我演示的是部署在公网服务器上的骰子猫桌游馆系统。系统面向单店桌游馆经营，顾客可以在线预约、浏览桌游、查看租借和排行榜；店长和员工可以在后台处理桌位、会员、桌游资产、租借和经营分析。” |
| 00:15-00:35 | 切换服务器终端，展示 `docker compose ps` | “先看运行环境。线上由 Caddy、Web、API、MySQL 四个容器组成。Caddy 负责 HTTPS 和反向代理，Web 提供前端页面，API 运行 Express 服务，MySQL 保存业务数据。这个项目可以直接在云服务器用 Docker Compose 构建和启动。” |
| 00:35-00:50 | 打开 `/api/health` | “这里是健康检查接口，可以看到 API、数据库、迁移和 AI 配置状态。部署后可以通过这个入口判断服务状态，也方便定位数据库或大模型配置问题。” |
| 00:50-01:25 | 回到顾客首页，展示预约表单、排行榜、租借、桌游目录和 AI 导购入口 | “顾客端包含首页预约、顾客登录、我的预约、玩家排行榜、桌游租借、桌游目录和 AI 导购。预约时用户填写人数和时间，后端结合桌位容量、当前状态和预约冲突查询可用桌位。排行榜、目录热度和租借信息都来自数据库。” |
| 01:25-01:50 | 用顾客账号登录，展示“我的预约”和战绩入口 | “顾客登录后可以看到自己的预约记录。系统支持围绕自己的预约提交战绩，后端会校验预约归属、场次状态和是否重复提交。顾客侧形成了注册登录、预约、查看记录、提交战绩的完整流程。” |
| 01:50-02:12 | 点击 AI 导购，提问“4个人新手适合玩什么，有空桌吗？” | “AI 导购会先读取当前空桌、桌游目录、人数范围、近期热度和规则资料，再生成推荐。它会说明推荐原因，也会提醒用户需要自己点击提交预约。AI 负责导购和解释，业务写入由用户确认。” |
| 02:12-02:40 | 进入 `https://lhywork.top/admin`，登录后台，展示运营总览 | “后台首页是门店运营总览。这里汇总今日收入、空闲桌位、预约中、占用中、活跃会员、租借逾期和热门桌游。店长打开后台后，可以先看经营状态，再决定处理桌位、会员或租借问题。” |
| 02:40-03:05 | 展示 AI 经营大脑五段流程、风险卡片和建议动作 | “AI 经营大脑采用工具型智能体流程：数据接入、工具检索、模型推理、风险研判、建议动作。它读取桌位、预约、会员、租借、订单和热门桌游，输出风险摘要和运营建议，例如处理逾期租借、引导顾客预约空桌、关注即将到时的桌位。” |
| 03:05-03:28 | 切到“桌位预约”和“对局状态” | “桌位模块用于员工日常操作。系统区分空闲、预约中和占用中状态，可以处理预约到店、现场开台、查看进行中场次和超时提醒。对局状态用于跟踪当前桌位的进行情况，方便员工决定续时、结算或提醒顾客。” |
| 03:28-03:52 | 切到“会员管理”“优惠券”“订单与计费” | “会员模块维护会员号、手机号、余额、积分、等级、储值和消费记录。优惠券用于营销活动和折扣核销，订单与计费记录沉淀结算金额、折扣和积分变化，这些数据会继续进入运营总览和报表。” |
| 03:52-04:17 | 切到“桌游目录”和“桌游租借” | “桌游目录可以维护封面、描述、人数范围、时长、难度和分类，顾客端和后台共用同一份数据库目录。租借模块管理实体副本，包括条码、位置、押金、品相、借出、归还、逾期和丢失状态。” |
| 04:17-04:36 | 切到“数据报表”“员工与权限” | “数据报表包含桌游热度、桌位利用率和经营趋势，用于支撑店长决策。员工与权限保留店长和员工两级角色，店长可以管理员工和资产，员工负责预约、开台、会员基础操作和借还。” |
| 04:36-04:52 | 打开代码目录，展示 `server/src/routes`、`services`、`db/migrations`、`scripts` | “工程结构上，后端按 routes、services、auth、security、audit、db 拆分；数据库有初始化脚本、迁移脚本、视图、存储过程和迁移记录；脚本层有数据库检查、备份、部署检查和演示数据补齐。” |
| 04:52-05:00 | 回到公网后台总览做总结 | “整体上，系统覆盖顾客预约、后台运营、会员、桌游目录、租借、权限、报表、AI 分析和部署运维，把桌游馆经营数据串成了一套可运行、可维护、可扩展的单店系统。” |

## 功能覆盖提醒

录制时可以按侧栏快速扫过这些功能，避免漏讲：

- 顾客端：首页预约、顾客注册登录、我的预约、战绩提交、AI 导购、排行榜、桌游租借、桌游目录。
- 后台端：运营总览、AI 经营大脑、桌位预约、会员管理、对局状态、数据报表、桌游目录、优惠券、订单与计费、桌游租借、员工与权限。
- 工程端：公网 HTTPS、Docker Compose、健康检查、数据库迁移、备份脚本、部署检查、演示数据脚本、审计日志、权限校验。

## 录制中可用的服务器命令

```bash
cd /opt/boardgame-system

# 查看容器状态
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod ps

# 查看 API 日志
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod logs --tail=80 api

# 补齐演示数据
docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod exec api npm run demo:seed

# 健康检查
curl -k https://lhywork.top/api/health
```

## 录制异常兜底话术

- AI 调用失败：大模型接口依赖外部服务，核心预约、会员、租借和后台运营功能仍然由本系统数据库支撑。
- 页面数据少：执行 `npm run demo:seed` 补齐今日预约、会员、租借和收入演示数据。
- 登录态保留：后台使用 Bearer Token 本地缓存，便于员工连续操作；后续可以扩展为 HttpOnly Cookie。
- 容器异常：通过 `/api/health` 和 `docker compose logs api` 查看服务状态和错误日志。
